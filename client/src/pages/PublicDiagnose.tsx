import { Button } from "@/components/ui/button";
import { formatBytes, formatDuration } from "@/lib/format";
import { AlertTriangle, Archive, CheckCircle2, ChevronDown, CircleCheck, Clock3, Download, FileArchive, FileJson2, FileText, FileUp, Folder, History, Loader2, Plus, RotateCcw, Settings2, ShieldCheck, Trash2, UploadCloud, X } from "lucide-react";
import { useEffect, useRef, useState } from "react";
import { toast } from "sonner";
import { startLocalScan } from "@/scanner/client";
import { detectArchiveFormat, scanLimits } from "@/scanner/security";
import { clearLocalReports, listLocalReports, loadLocalSignatures, removeLocalReport, saveLocalReport, saveLocalSignatures } from "@/scanner/history";
import { exportJson, exportPdf, exportTxt } from "@/scanner/export";
import type { ArchiveNode, ScanReport, SignatureDefinition, WorkerEvent, WorkerProgress } from "@/scanner/types";

type Tab = "scanner" | "history" | "signatures";
type ProgressState = WorkerProgress & { startedAt: number };

const stages = [
  "Detectando formato", "Extraindo arquivo", "Localizando arquivos relevantes", "Analisando McSettings", "Analisando McSettingsEvents", "Analisando McProfile", "Analisando SystemProfile", "Analisando certificados", "Correlacionando evidências", "Finalizando relatório",
];

const labelForResult = { clean: "Nenhuma evidência conclusiva", suspicious: "Revisão recomendada", evidence: "Evidências com contexto" } as const;
const classForResult = { clean: "border-emerald-300/30 bg-emerald-300/10 text-emerald-100", suspicious: "border-amber-300/30 bg-amber-300/10 text-amber-100", evidence: "border-orange-300/30 bg-orange-300/10 text-orange-100" } as const;

function activeStageIndex(stage: string) {
  const value = stage.toLowerCase();
  if (value.includes("final") || value.includes("relat")) return 9;
  if (value.includes("correl")) return 8;
  if (value.includes("cert")) return 7;
  if (value.includes("systemprofile")) return 6;
  if (value.includes("mcprofile")) return 5;
  if (value.includes("mcsettingsevents")) return 4;
  if (value.includes("mcsettings")) return 3;
  if (value.includes("analis")) return 3;
  if (value.includes("localiz")) return 2;
  if (value.includes("extra")) return 1;
  return 0;
}

function LocalNotice() {
  return <div className="mt-6 flex gap-3 rounded-xl border border-cyan-300/15 bg-cyan-300/[.04] p-4 text-xs leading-5 text-slate-400"><ShieldCheck className="mt-0.5 h-4 w-4 shrink-0 text-cyan-200" /><p><b className="text-slate-200">Privacidade local.</b> O Sysdiagnose é aberto e analisado no seu dispositivo por um Web Worker. O arquivo não é enviado a um servidor e não é salvo no histórico.</p></div>;
}

function ProgressPanel({ progress, onCancel }: { progress: ProgressState; onCancel: () => void }) {
  const index = activeStageIndex(progress.stage);
  const elapsed = Date.now() - progress.startedAt;
  const percentage = progress.progress;
  return <section className="mt-8 overflow-hidden rounded-2xl border border-cyan-300/15 bg-[#0e1622] shadow-[0_20px_60px_rgba(0,0,0,.24)]"><div className="border-b border-white/[.07] px-5 py-5 sm:px-7"><div className="flex items-start justify-between gap-5"><div><p className="eyebrow">ANÁLISE LOCAL EM ANDAMENTO</p><h2 className="mt-2 text-xl font-semibold text-slate-100">{progress.stage}</h2><p className="mt-1 text-xs text-slate-500">O estado e os contadores abaixo refletem o trabalho já executado no dispositivo.</p></div><div className="text-right"><p className="font-mono text-xl font-semibold text-cyan-200">{typeof percentage === "number" ? `${percentage}%` : "ETAPA"}</p><p className="mt-1 text-[10px] uppercase tracking-[.12em] text-slate-500">{typeof percentage === "number" ? "leitura do arquivo" : "em execução"}</p></div></div>{typeof percentage === "number" && <div className="mt-5 h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-cyan-300 transition-[width] duration-300" style={{ width: `${Math.max(1, percentage)}%` }} /></div>}<div className="mt-4 flex flex-wrap items-center justify-between gap-3 text-xs"><span className="text-cyan-100"><Loader2 className="mr-1.5 inline h-3.5 w-3.5 animate-spin" />{progress.bytesRead ? `${formatBytes(progress.bytesRead)} de ${formatBytes(progress.totalBytes)} lidos` : "Preparando leitura local"}</span><span className="font-mono text-slate-500">decorrido {formatDuration(elapsed)}</span></div></div><ol className="divide-y divide-white/[.06]">{stages.map((label, stageIndex) => { const done = stageIndex < index; const current = stageIndex === index; return <li key={label} className={`flex gap-4 px-5 py-3.5 sm:px-7 ${current ? "bg-cyan-300/[.035]" : ""}`}><span className={`grid h-7 w-7 shrink-0 place-items-center rounded-full border font-mono text-[10px] ${done ? "border-cyan-300/25 bg-cyan-300/10 text-cyan-100" : current ? "border-cyan-300/50 text-cyan-100" : "border-slate-700 text-slate-600"}`}>{done ? <CircleCheck className="h-4 w-4" /> : String(stageIndex + 1).padStart(2, "0")}</span><div className="min-w-0"><p className={`text-sm font-medium ${done || current ? "text-slate-100" : "text-slate-500"}`}>{label}</p>{current && <p className="mt-1 text-xs text-slate-500">{progress.stage}</p>}</div></li>; })}</ol><div className="grid grid-cols-2 gap-px border-t border-white/[.07] bg-white/[.07]"><div className="bg-[#0e1622] px-5 py-4 sm:px-7"><p className="text-[10px] font-semibold uppercase tracking-[.12em] text-slate-500">Arquivos analisados</p><p className="mt-1 text-lg font-semibold text-slate-100">{progress.processedFileCount}</p></div><div className="bg-[#0e1622] px-5 py-4 sm:px-7"><p className="text-[10px] font-semibold uppercase tracking-[.12em] text-slate-500">Arquivos relevantes</p><p className="mt-1 text-lg font-semibold text-slate-100">{progress.relevantFileCount}</p></div></div><div className="flex justify-end border-t border-white/[.07] px-5 py-4"><Button onClick={onCancel} variant="outline" className="border-slate-700 bg-transparent text-slate-300 hover:bg-white/[.05]"><X className="mr-2 h-4 w-4" />Cancelar análise</Button></div></section>;
}

function ArchiveTree({ nodes, selected, onSelect }: { nodes: ArchiveNode[]; selected: string | null; onSelect: (path: string) => void }) {
  const draw = (items: ArchiveNode[], level = 0): React.ReactNode => items.map(node => <div key={node.id}><button onClick={() => onSelect(node.path)} className={`flex w-full items-center gap-2 rounded-lg px-2 py-1.5 text-left text-xs transition-colors ${selected === node.path ? "bg-cyan-300/10 text-cyan-100" : "text-slate-400 hover:bg-white/[.04]"}`} style={{ paddingLeft: `${level * 15 + 8}px` }}>{node.type === "directory" ? <Folder className="h-3.5 w-3.5 text-cyan-200/70" /> : <FileText className="h-3.5 w-3.5 text-slate-500" />}<span className="min-w-0 flex-1 truncate">{node.name}</span>{node.type === "file" && typeof node.size === "number" ? <span className="font-mono text-[10px] text-slate-600">{formatBytes(node.size)}</span> : null}</button>{node.children?.length ? draw(node.children, level + 1) : null}</div>);
  return <div id="archive-tree" className="mt-3 max-h-80 overflow-auto rounded-xl border border-white/[.08] bg-black/10 p-2">{nodes.length ? draw(nodes) : <p className="p-4 text-xs text-slate-500">A estrutura disponível não contém entradas exibíveis.</p>}</div>;
}

function HistoryView({ reports, onOpen, onRemove, onClear }: { reports: ScanReport[]; onOpen: (item: ScanReport) => void; onRemove: (id: string) => void; onClear: () => void }) {
  return <section className="py-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">HISTÓRICO LOCAL</p><h1 className="mt-3 text-3xl font-semibold text-slate-50">Análises neste dispositivo</h1><p className="mt-3 text-sm text-slate-400">Apenas dados de relatório são guardados em IndexedDB. O Sysdiagnose não é armazenado.</p></div>{reports.length > 0 && <Button variant="outline" onClick={onClear} className="border-slate-700 bg-transparent text-slate-300"><Trash2 className="mr-2 h-4 w-4" />Limpar histórico</Button>}</div>{reports.length ? <div className="mt-7 space-y-3">{reports.map(item => <article key={item.id} className="flex flex-col gap-4 rounded-xl border border-white/[.08] bg-[#0e1622] p-4 sm:flex-row sm:items-center"><FileArchive className="h-5 w-5 shrink-0 text-cyan-200" /><button onClick={() => onOpen(item)} className="min-w-0 flex-1 text-left"><p className="truncate text-sm font-medium text-slate-100">{item.fileName}</p><p className="mt-1 text-xs text-slate-500">{new Date(item.createdAt).toLocaleString()} · {formatBytes(item.fileSize)} · {item.evidence.length} evidência(s)</p></button><span className={`w-fit rounded-full border px-2.5 py-1 text-[11px] font-semibold ${classForResult[item.result]}`}>{item.overallConfidence}</span><button onClick={() => onRemove(item.id)} className="w-fit text-slate-500 hover:text-red-200" aria-label="Remover relatório"><Trash2 className="h-4 w-4" /></button></article>)}</div> : <div className="mt-7 rounded-xl border border-dashed border-slate-700 p-10 text-center"><History className="mx-auto h-5 w-5 text-slate-600" /><p className="mt-3 text-sm text-slate-300">Nenhuma análise local ainda.</p></div>}</section>;
}

function SignaturesView({ signatures, onChange }: { signatures: SignatureDefinition[]; onChange: (items: SignatureDefinition[]) => void }) {
  const importRef = useRef<HTMLInputElement>(null);
  const [draft, setDraft] = useState("");
  const commit = (items: SignatureDefinition[]) => { onChange(items); void saveLocalSignatures(items); };
  const add = () => { try { const item = JSON.parse(draft) as SignatureDefinition; if (!item.id || !item.indicator || !item.name) throw new Error(); commit([...signatures, { ...item, enabled: item.enabled !== false }]); setDraft(""); toast.success("Assinatura local adicionada."); } catch { toast.error("Informe um objeto JSON válido com id, name e indicator."); } };
  return <section className="py-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">ASSINATURAS LOCAIS</p><h1 className="mt-3 text-3xl font-semibold text-slate-50">Regras usadas neste navegador</h1><p className="mt-3 text-sm text-slate-400">A edição ocorre somente neste dispositivo e não altera a base pública do projeto.</p></div><div className="flex flex-wrap gap-2"><Button variant="outline" onClick={() => { const url = URL.createObjectURL(new Blob([JSON.stringify(signatures, null, 2)], { type: "application/json" })); const anchor = document.createElement("a"); anchor.href = url; anchor.download = "signatures.json"; anchor.click(); URL.revokeObjectURL(url); }} className="border-slate-700 bg-transparent text-slate-300"><Download className="mr-2 h-4 w-4" />Exportar</Button><Button variant="outline" onClick={() => importRef.current?.click()} className="border-slate-700 bg-transparent text-slate-300"><UploadCloud className="mr-2 h-4 w-4" />Importar</Button><input ref={importRef} type="file" accept="application/json" className="hidden" onChange={async event => { const source = event.target.files?.[0]; if (!source) return; try { const incoming = JSON.parse(await source.text()) as SignatureDefinition[]; if (!Array.isArray(incoming)) throw new Error(); commit(incoming); toast.success("Assinaturas locais importadas."); } catch { toast.error("O arquivo não contém uma lista JSON válida de assinaturas."); } }} /></div></div><div className="mt-7 grid gap-5 lg:grid-cols-[1fr_320px]"><div className="space-y-3">{signatures.map(item => <article key={item.id} className="flex gap-3 rounded-xl border border-white/[.08] bg-[#0e1622] p-4"><button aria-label="Ativar ou desativar assinatura" onClick={() => commit(signatures.map(value => value.id === item.id ? { ...value, enabled: value.enabled === false } : value))} className={`mt-0.5 h-5 w-9 rounded-full p-0.5 transition-colors ${item.enabled === false ? "bg-slate-700" : "bg-cyan-300/70"}`}><span className={`block h-4 w-4 rounded-full bg-white transition-transform ${item.enabled === false ? "translate-x-0" : "translate-x-4"}`} /></button><div className="min-w-0 flex-1"><p className="text-sm font-medium text-slate-100">{item.name}</p><p className="mt-1 font-mono text-xs text-cyan-100/80">{item.indicator}</p><p className="mt-2 text-xs leading-5 text-slate-500">{item.category} · {item.type} · {item.isWeak ? "indicador fraco" : "contextual"}</p></div><button onClick={() => commit(signatures.filter(value => value.id !== item.id))} className="h-fit text-slate-600 hover:text-red-200" aria-label="Remover assinatura"><Trash2 className="h-4 w-4" /></button></article>)}</div><aside className="rounded-xl border border-white/[.08] bg-[#0e1622] p-4"><p className="text-sm font-medium text-slate-100">Adicionar assinatura local</p><p className="mt-2 text-xs leading-5 text-slate-500">Cole um objeto JSON compatível. Indicadores curtos devem ser marcados com `isWeak: true`.</p><textarea value={draft} onChange={event => setDraft(event.target.value)} placeholder={'{"id":"minha-regra","name":"Nome","category":"Categoria","indicator":"valor","type":"string","severity":"baixa","baseConfidence":"baixa","expectedFiles":[],"expectedKeys":[],"contextExpected":[],"description":"...","isWeak":true,"weight":1}'} className="mt-4 min-h-52 w-full rounded-lg border border-white/[.08] bg-black/20 p-3 font-mono text-[11px] text-slate-300 outline-none focus:border-cyan-300/50" /><Button onClick={add} className="mt-3 w-full bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Plus className="mr-2 h-4 w-4" />Adicionar localmente</Button></aside></div></section>;
}

export default function PublicDiagnose() {
  const input = useRef<HTMLInputElement>(null);
  const cancelScan = useRef<(() => void) | null>(null);
  const scanStartedAt = useRef<number | null>(null);
  const [tab, setTab] = useState<Tab>("scanner");
  const [file, setFile] = useState<File | null>(null);
  const [format, setFormat] = useState<"tar.gz" | "zip" | null>(null);
  const [processing, setProcessing] = useState(false);
  const [progress, setProgress] = useState<ProgressState | null>(null);
  const [report, setReport] = useState<ScanReport | null>(null);
  const [reports, setReports] = useState<ScanReport[]>([]);
  const [signatures, setSignatures] = useState<SignatureDefinition[]>([]);
  const [selectedPath, setSelectedPath] = useState<string | null>(null);
  const [draggingFile, setDraggingFile] = useState(false);

  useEffect(() => { void (async () => { try { const defaults = await fetch("/data/signatures.json").then(response => response.json() as Promise<SignatureDefinition[]>); const local = await loadLocalSignatures<SignatureDefinition[]>(); setSignatures(local?.length ? local : defaults); setReports(await listLocalReports()); } catch { toast.error("Não foi possível carregar a base local de assinaturas."); } })(); return () => cancelScan.current?.(); }, []);
  const reset = () => { cancelScan.current?.(); cancelScan.current = null; scanStartedAt.current = null; setFile(null); setFormat(null); setProgress(null); setReport(null); setProcessing(false); setSelectedPath(null); setDraggingFile(false); if (input.current) input.current.value = ""; };
  const selectFile = async (candidate?: File) => { if (!candidate) return; try { if (candidate.size <= 0 || candidate.size > scanLimits.maxUploadBytes) throw new Error("O arquivo precisa ter entre 1 byte e 350 MB."); const detected = await detectArchiveFormat(candidate); setFile(candidate); setFormat(detected); setReport(null); setProgress(null); } catch (error) { toast.error(error instanceof Error ? error.message : "Arquivo inválido."); } };
  const onEvent = (event: WorkerEvent) => { if (event.type === "progress") setProgress({ ...event, startedAt: scanStartedAt.current ?? Date.now() }); else if (event.type === "complete") { scanStartedAt.current = null; setReport(event.report); setProcessing(false); setProgress(null); void saveLocalReport(event.report); void listLocalReports().then(setReports); } else if (event.type === "error") { scanStartedAt.current = null; setProcessing(false); setProgress(null); toast.error(event.message); } else { scanStartedAt.current = null; setProcessing(false); setProgress(null); toast.message("Análise local cancelada."); } };
  const start = () => { if (!file || !signatures.length) return toast.error("Aguarde o carregamento das assinaturas locais."); const startedAt = Date.now(); scanStartedAt.current = startedAt; setProcessing(true); setProgress({ type: "progress", step: 1, stage: "Iniciando análise no dispositivo", progress: 0, processedFileCount: 0, relevantFileCount: 0, bytesRead: 0, totalBytes: file.size, startedAt }); cancelScan.current = startLocalScan(file, signatures, onEvent); };
  const cancel = () => { cancelScan.current?.(); cancelScan.current = null; scanStartedAt.current = null; setProcessing(false); setProgress(null); };
  const openReport = (item: ScanReport) => { setReport(item); setTab("scanner"); setSelectedPath(null); };
  const navigation: Array<[Tab, string, React.ReactNode]> = [["scanner", "Scanner", <Archive className="h-4 w-4" />], ["history", "Histórico local", <History className="h-4 w-4" />], ["signatures", "Assinaturas locais", <Settings2 className="h-4 w-4" />]];

  return <main className="min-h-screen bg-[#080d14] px-4 py-7 text-slate-100 sm:px-6 sm:py-10"><div className="mx-auto max-w-5xl"><header className="flex flex-col justify-between gap-5 border-b border-white/[0.08] pb-5 sm:flex-row sm:items-center"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl border border-cyan-300/20 bg-cyan-300/10"><ShieldCheck className="h-5 w-5 text-cyan-200" /></div><div><p className="text-sm font-semibold tracking-[.16em] text-slate-100">RX7</p><p className="mt-0.5 text-[10px] tracking-[.1em] text-slate-500">iOS SYS DIAGNOSE SCANNER</p></div></div><span className="flex items-center gap-2 text-xs text-cyan-100/80"><ShieldCheck className="h-3.5 w-3.5" />Análise local no dispositivo</span></header><nav className="mt-5 flex gap-2 overflow-x-auto border-b border-white/[.06] pb-4">{navigation.map(([id, label, icon]) => <button key={id} onClick={() => setTab(id)} className={`inline-flex shrink-0 items-center gap-2 rounded-lg px-3 py-2 text-xs font-medium transition-colors ${tab === id ? "bg-cyan-300 text-slate-950" : "text-slate-400 hover:bg-white/[.05] hover:text-slate-100"}`}>{icon}{label}</button>)}</nav>{tab === "history" ? <HistoryView reports={reports} onOpen={openReport} onRemove={id => void removeLocalReport(id).then(() => listLocalReports().then(setReports))} onClear={() => void clearLocalReports().then(() => setReports([]))} /> : tab === "signatures" ? <SignaturesView signatures={signatures} onChange={setSignatures} /> : !report ? <section className="py-11 sm:py-14"><p className="eyebrow">SYS DIAGNOSE · IPHONE / iOS</p><h1 className="mt-3 text-3xl font-semibold tracking-tight text-slate-50 sm:text-4xl">Analisar Sysdiagnose</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-slate-400">Selecione um Sysdiagnose para extrair, localizar arquivos relevantes e aplicar regras contextuais diretamente no navegador. A análise não trata sequências isoladas como diagnóstico.</p>{processing && progress ? <ProgressPanel progress={progress} onCancel={cancel} /> : <><input ref={input} type="file" accept=".tar.gz,.tgz,.zip" className="hidden" onChange={event => void selectFile(event.target.files?.[0])} /><button onClick={() => input.current?.click()} onDragEnter={event => { event.preventDefault(); setDraggingFile(true); }} onDragOver={event => event.preventDefault()} onDragLeave={event => { event.preventDefault(); setDraggingFile(false); }} onDrop={event => { event.preventDefault(); setDraggingFile(false); void selectFile(event.dataTransfer.files?.[0]); }} className={`mt-8 flex min-h-60 w-full flex-col items-center justify-center rounded-2xl border border-dashed bg-[#0e1622] px-6 text-center transition-colors ${draggingFile ? "border-cyan-200 bg-cyan-300/[.07]" : "border-slate-600/80 hover:border-cyan-300/45"}`}><div className="grid h-14 w-14 place-items-center rounded-2xl bg-cyan-300/[.08] text-cyan-200"><UploadCloud className="h-6 w-6" /></div><p className="mt-5 text-base font-medium text-slate-100">Arraste ou selecione um Sysdiagnose</p><p className="mt-2 text-xs leading-5 text-slate-500">.tar.gz, .tgz ou .zip · até 350 MB · processado localmente</p></button><p className="mt-3 text-xs leading-5 text-slate-500">Arquivos grandes dependem da memória disponível do navegador. Plists binários são identificados e reportados como limitação, sem leitura simulada.</p>{file && <div className="mt-4 flex flex-col gap-4 rounded-xl border border-cyan-300/15 bg-cyan-300/[.04] p-4 sm:flex-row sm:items-center"><FileArchive className="h-5 w-5 shrink-0 text-cyan-200" /><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium text-slate-200">{file.name}</p><p className="mt-1 text-xs text-slate-500">{formatBytes(file.size)} · formato detectado: {format === "tar.gz" ? "TAR.GZ" : "ZIP"}</p></div><button className="text-xs text-slate-400 hover:text-slate-100" onClick={reset}>Remover</button></div>}<div className="mt-6 flex flex-col items-start justify-between gap-4 border-t border-white/[.07] pt-5 sm:flex-row sm:items-center"><p className="max-w-md text-xs leading-5 text-slate-500">Indicadores curtos ou isolados permanecem como baixa confiança. A confiança aumenta apenas com arquivo, chave e contexto verificáveis.</p><Button disabled={!file || !signatures.length} onClick={start} className="w-full bg-cyan-300 text-slate-950 hover:bg-cyan-200 sm:w-auto"><FileUp className="mr-2 h-4 w-4" />Iniciar análise local</Button></div></>}<LocalNotice /></section> : <section className="py-10"><div className="flex flex-col gap-5 border-b border-white/[.08] pb-7 sm:flex-row sm:items-start sm:justify-between"><div><p className="eyebrow">RELATÓRIO LOCAL DE VARREDURA</p><h1 className="mt-3 break-all text-2xl font-semibold tracking-tight text-slate-50">{report.fileName}</h1><p className="mt-2 flex flex-wrap gap-x-2 text-sm text-slate-500"><span>{formatBytes(report.fileSize)}</span><span>·</span><span>{formatDuration(report.durationMs)}</span><span>·</span><span>{report.processedFileCount} arquivo(s) analisado(s)</span></p></div><span className={`w-fit rounded-full border px-3 py-1.5 text-xs font-semibold ${classForResult[report.result]}`}>{labelForResult[report.result]}</span></div><div className="mt-6 grid gap-3 sm:grid-cols-4">{[["Confiança geral", report.overallConfidence], ["Score de confiança", `${report.score}/100`], ["Evidências", String(report.evidence.length)], ["Arquivos relevantes", String(report.relevantFileCount)]].map(([label, value]) => <div key={label} className="rounded-xl border border-white/[.08] bg-[#0e1622] p-4"><p className="text-[10px] font-semibold uppercase tracking-[.12em] text-slate-500">{label}</p><p className="mt-3 text-lg font-semibold capitalize text-slate-100">{value}</p></div>)}</div><div className="mt-7 flex flex-wrap gap-2"><Button variant="outline" onClick={() => exportJson(report)} className="border-slate-700 bg-transparent text-slate-300"><FileJson2 className="mr-2 h-4 w-4" />JSON</Button><Button variant="outline" onClick={() => exportTxt(report)} className="border-slate-700 bg-transparent text-slate-300"><FileText className="mr-2 h-4 w-4" />TXT</Button><Button variant="outline" onClick={() => exportPdf(report)} className="border-slate-700 bg-transparent text-slate-300"><Download className="mr-2 h-4 w-4" />PDF</Button></div><div className="mt-8"><p className="eyebrow">EVIDÊNCIAS ENCONTRADAS</p>{report.evidence.length ? <div className="mt-3 space-y-3">{report.evidence.map((item, index) => <details key={`${item.signatureId}-${index}`} className="group rounded-xl border border-white/[.08] bg-[#0e1622] px-4"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 py-4"><button onClick={event => { event.preventDefault(); setSelectedPath(item.sourcePath); document.getElementById("archive-tree")?.scrollIntoView({ behavior: "smooth", block: "center" }); }} className="min-w-0 text-left"><p className="text-sm font-medium text-slate-100">{item.signature}</p><p className="mt-1 truncate text-xs text-slate-500">{item.sourcePath}</p></button><ChevronDown className="h-4 w-4 shrink-0 text-slate-500 transition-transform group-open:rotate-180" /></summary><div className="border-t border-white/[.07] py-4 text-xs leading-5 text-slate-400"><div className="grid gap-x-6 gap-y-2 sm:grid-cols-2"><p><b className="text-slate-200">Categoria:</b> {item.category}</p><p><b className="text-slate-200">Confiança:</b> {item.confidence}</p><p><b className="text-slate-200">Chave:</b> {item.plistKey ?? "não estruturada"}</p><p><b className="text-slate-200">Valor:</b> {item.value}</p></div><p className="mt-3"><b className="text-slate-200">Por que apareceu:</b> {item.reason}</p><pre className="mt-3 max-h-32 overflow-auto rounded-lg border border-white/[.06] bg-black/20 p-3 whitespace-pre-wrap break-words text-[11px] text-slate-500">{item.context || "Contexto textual não disponível."}</pre></div></details>)}</div> : <div className="mt-3 rounded-xl border border-dashed border-slate-700 p-7 text-center"><CheckCircle2 className="mx-auto h-5 w-5 text-emerald-200" /><p className="mt-3 text-sm text-slate-200">Nenhuma evidência contextual encontrada</p><p className="mt-1 text-xs leading-5 text-slate-500">Isso é uma triagem local e não substitui uma análise independente.</p></div>}</div><div className="mt-8"><p className="eyebrow">ÁRVORE DO SYSDIAGNOSE</p><p className="mt-2 text-xs text-slate-500">Clique em um arquivo para localizá-lo; clicar em uma evidência destaca sua origem.</p><ArchiveTree nodes={report.tree} selected={selectedPath} onSelect={setSelectedPath} /></div>{report.limitations.length > 0 && <div className="mt-7 rounded-xl border border-amber-300/15 bg-amber-300/[.04] p-5"><AlertTriangle className="h-4 w-4 text-amber-100" /><p className="mt-3 text-sm font-medium text-amber-100">Limitações identificadas</p><ul className="mt-2 list-disc space-y-2 pl-4 text-xs leading-5 text-amber-100/65">{report.limitations.map(item => <li key={item}>{item}</li>)}</ul></div>}<div className="mt-7 rounded-xl border border-amber-300/15 bg-amber-300/[.04] p-5"><AlertTriangle className="h-4 w-4 text-amber-100" /><p className="mt-3 text-sm font-medium text-amber-100">Investigação segura</p><ul className="mt-2 space-y-2 text-xs leading-5 text-amber-100/65">{report.recommendations.map(item => <li key={item}>{item}</li>)}</ul></div><div className="mt-7 flex flex-wrap gap-3"><Button onClick={reset} variant="outline" className="border-slate-700 bg-transparent text-slate-200 hover:bg-white/[.05]"><RotateCcw className="mr-2 h-4 w-4" />Novo Diagnose</Button><span className="inline-flex items-center gap-2 text-xs text-slate-500"><Clock3 className="h-3.5 w-3.5" />Gerado localmente em {new Date(report.createdAt).toLocaleString()}</span></div></section>}<footer className="border-t border-white/[.07] py-5 text-center text-[11px] text-slate-600">Developed by RX7 · análise contextual local de SYSdiagnose</footer></div></main>;
}
